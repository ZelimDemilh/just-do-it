const {Router} = require("express")

const router = Router()

router.post("/add", ) // Добавление пользователя
router.post("/login", ) // Отправление данных пользователя
router.post("/profile", ) // Получение данных пользователя
router.delete("/remove", ) // Удаление пользователя

module.exports = router